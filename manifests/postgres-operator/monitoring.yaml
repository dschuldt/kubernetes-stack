---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: demo
  namespace: default
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: demo
  podMetricsEndpoints:
    - port: metrics
